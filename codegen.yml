overwrite: true
schema: "./src/server/schema.graphql"
generates:
  ./src/server/gql-schema-types.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      skipTypename: true
